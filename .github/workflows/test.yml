name: Scheduler tests

on: 
  push:
    branch: [master]
  pull_request:
    branch: [master]

jobs:
  Tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with: 
        submodules: true
    - name: Build
      run: |
        mkdir build
        cd build
        cmake ..
        make -j4
    - name: Test
      run: |
        cd build
        make test
    - name: Example
      run: |
        cd build
        ./example
